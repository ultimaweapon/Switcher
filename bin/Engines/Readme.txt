This directory contains all of available switching engine. Each engine will be in a separate sub-directory.
The name of sub-directory will be display name of the engine. Each sub-directory will contains at least Engine.ini.
You can find more engines on the internet. But if you want to develop one, take a look at SDK directory.